function solve(fruitType,weight,priceForKilogram){
    let result = (weight/1000)*priceForKilogram;
    console.log(`I need $${result.toFixed(2)} to buy ${(weight/1000).toFixed(2)} kilograms ${fruitType}.`);
}

solve('orange', 2500, 1.80)